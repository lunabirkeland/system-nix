{
  programs.nvf.settings.vim = {
    session = {
      nvim-session-manager = {
        enable = true;
        setupOpts = {
          autoload_mode = "CurrentDir";
        };
      };
    };
  };
}
